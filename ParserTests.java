import static org.junit.Assert.assertEquals;
import org.junit.Test;

public class ParserTests {

    /*
    @Test
    public void testexp() {
        Statement root = null;
        try{
            Parser p = new Parser("testfiles/t1.bpl");
            root = p.parse();
        } catch (Exception e) {
            System.out.println(e);
            assertEquals(true,false);
        }
    }

    @Test
    public void testcompound() {
        Statement root = null;
        try{
            Parser p = new Parser("testfiles/t2.bpl");
            root = p.parse();
        } catch (Exception e) {
            System.out.println(e);
            assertEquals(true,false);
        }
        assertEquals(root.type, TreeNode.STATEMENT);
        assertEquals(root.cs.type, TreeNode.COMPOUND_STMT);
        assertEquals(root.cs.sl.type, TreeNode.STATEMENT_LIST);
        //assertEquals(root.cs.sl.head.next.es.expression.var.value, "y");
    }
    
    @Test
    public void testwhile() {
        try{
            Parser p = new Parser("testfiles/t3.bpl");
            ParseNode root = p.parse();
        } catch (Exception e) {
            System.out.println(e);
            assertNotReached();
        }
    }
    */
}
