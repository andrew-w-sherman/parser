while ( t * -f >= &p + -u * v ) {
    a = 2;
    str =/*whoa*/ "here's the mail it never fails";
    funtest = foo(read(), 4 * 5, bar());
    c = d + (ds = 4 + 5 * read());
}

/* idk here's a
comment */
